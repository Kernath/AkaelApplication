@model IEnumerable<AkaelApplication.Models.PraticaEspecificaProdutoTrabalho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PraticaEspecifica.Sigla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProdutoTrabalho.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PraticaEspecifica.Sigla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProdutoTrabalho.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPraticaEspecificaProdutoTrabalho }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPraticaEspecificaProdutoTrabalho }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPraticaEspecificaProdutoTrabalho })
        </td>
    </tr>
}

</table>
